using System;

namespace zrok
{
    public enum Direction
    {
        North,
        South,
        East,
        West,
        Up,
        Down
    }

    public enum WT {
        NOUN,
        VERB,
        ADJECTIVE,
        CONJUNCTION,
        ARTICLE,
        PREPOSITION,
        UNKNOWN,
        ERROR
    }
}
